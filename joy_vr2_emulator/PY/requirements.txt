# VR2 Bridge — Python dependencies
# Install: pip install -r requirements.txt

# BLE (Bluetooth Low Energy async client)
bleak>=0.21.0

# MQTT client
paho-mqtt>=1.6.1

# D-Bus BlueZ agent — optional, enables automatic BLE pairing on first run
# Without this, pair once manually with bluetoothctl before starting the bridge
dbus-next>=0.2.3
